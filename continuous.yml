deployment:
  hooks:
    AfterInstall:
      - command: echo "AfterInstall" 
    BeforeActivate:
      - command: echo "BeforeActivate" ; pwd > /tmp/test.log ;  chmod 755 ./vendor/bin/* ; ./vendor/bin/phing setup 
    AfterActivate:
      - command: echo "AfterActivate" 
