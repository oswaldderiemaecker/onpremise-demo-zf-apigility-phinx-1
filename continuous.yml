deployment:
  hooks:
    AfterInstall:
      - command: echo "afterinstall" 
    BeforeActivate:
      - command: echo "beforeactivate"
    AfterActivate:
      - command: echo "afteractivate" >> /tmp/test.log ; pwd >> /tmp/test.log ; id >> /tmp/test.log ; sudo chmod 755 ./vendor/bin/*
