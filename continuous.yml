deployment:
  hooks:
    AfterInstall:
      - command: echo 'the application is successfully installed'
    BeforeActivate:
      - command: pwd > /tmp/test.log ; chmod 755 ./vendor/bin/* ; ./vendor/bin/phing setup 
    AfterActivate:
      - command: echo 'the application has started'
