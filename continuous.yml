deployment:
  hooks:
    AfterInstall:
      - command: echo "AfterInstall" 
    BeforeActivate:
      - command: echo "BeforeActivate" ; cd /var/www/onpremise-demo-zf-apigility-phinx-1 ; ./vendor/bin/phing setup
    AfterActivate:
      - command: echo "AfterActivate" 
