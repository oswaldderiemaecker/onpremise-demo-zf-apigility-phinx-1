deployment:
  hooks:
    AfterInstall:
      - command: echo 'the application is successfully installed'
    BeforeActivate:
      - command: pwd > /tmp/test.log ; id >> /tmp/test.log ; sudo chmod 755 ./vendor/bin/* ; ./vendor/bin/phing setup 
    AfterActivate:
      - command: echo 'the application has started'
