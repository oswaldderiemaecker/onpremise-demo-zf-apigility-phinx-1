deployment:
  hooks:
    AfterInstall:
      - command: echo "AfterInstall" 
    BeforeActivate:
      - command: echo "BeforeActivate" 
    AfterActivate:
      - command: echo "AfterActivate" ; cd /var/www/onpremise-demo-zf-apigility-phinx-1/current ; ./vendor/bin/phing setup 
